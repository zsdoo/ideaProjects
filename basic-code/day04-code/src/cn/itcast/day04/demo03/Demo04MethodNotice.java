package cn.itcast.day04.demo03;
/* 方法注意事项：
1 定义在类中，不能嵌套。
2 顺序无所谓
3 不会自动执行，一定要调用：单独、打印、赋值调用。
4 如有返回值，必须加上“return 返回值 ”；
5 return 后面的返回值数据，必须和方法的返回值类型对应起来。
6 对于void没有返回值的方法，不能写返回值。return 只是结束。
7 方法中最后一行的return可以不写，如果有返回值则必须得写。
8 可以有多个return，但必须保证同时只有一个会被执行。
 */
public class Demo04MethodNotice {
}
